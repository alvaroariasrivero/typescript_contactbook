import * as React from "react";

type CardProps = {
  //
};

const Card: React.FC<any> = () => {
  return <div>
  <img/>
  <p>Name:</p>
  <p>Surname:</p>
  <p>Phone Number:</p>
</div>;;
};

export default Card;
