interface Contact {
    fname: string,
    surname: string,
    phone: number[],
    picture: string
  }; 

interface CardProps {
  contact: {
    fname:string, 
    surname: string, 
    phone: number[], 
    picture:string
  }
};