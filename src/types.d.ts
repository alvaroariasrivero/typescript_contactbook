type Contact = {
    fname: string,
    surname: string,
    phone: number[],
    picture: string
  }; 

type CardProps = {
  contact: {
    fname:string, 
    surname: string, 
    phone: number[], 
    picture:string
  }
};